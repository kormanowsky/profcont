stages:
  - build

build ubuntu:
  stage: build
  image: ubuntu
  before_script:
    - apt-get update -y
    - apt-get install -y cmake g++
  script:
    - cmake .
    - make
  artifacts:
    paths:
      - profcont
    expire_in: 30 days